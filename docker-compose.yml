version: '3.8'

services:
  web-mcp:
    build: .
    container_name: web-mcp-server
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
      - GOGGLES_URL=${GOGGLES_URL:-http://192.168.68.94:8003}
    networks:
      - web-mcp-network

networks:
  web-mcp-network:
    driver: bridge
